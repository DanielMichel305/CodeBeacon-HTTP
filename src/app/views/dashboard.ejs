<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Code beacon - Admin Dashboard</title>
    
</head>
<body style="background-color: #ffffff ;">
    <div style="display: flex; flex-direction: row; border: medium solid #16001F;">
    <% guilds.forEach(guild => { %>
        <div style=" border-radius: 5%; border-color: rgb(57, 45, 214); padding: 1%; margin: 15px; min-width: 5vw; min-height: 100px;">
            <%let iconImage = `https://cdn.discordapp.com/icons/${guild.id}/${guild.icon}.png`; if(!guild.icon) iconImage = 'https://cdn.discordapp.com/embed/avatars/0.png' %>

            <img style="border-radius: 20%;" src= <%-iconImage%> alt=<%-guild.name%> width="100" height="100">
            <% if(guild.existing_member) {%>
            <a style="background-color: rgb(82, 73, 207); padding: 5%; color: aliceblue;" href="http://localhost:8080/api/dashboard/<%-guild.id%>/"><%-guild.name%></a>
            <% } else { %>
                <a href="/invite/<%-guild.id%>"><%- `Invite to ${guild.name}` %></a>
            <% } %>
        </div>

    <% }) %>
    </div>


</body>
</html>